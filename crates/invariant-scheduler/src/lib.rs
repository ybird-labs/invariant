//! Scheduling primitives for the local execution layer.
//!
//! The [`domain`] module holds the pure, dependency-free core: jobs, their
//! state machine, scheduling value objects, and the errors they can produce.

pub mod domain;
